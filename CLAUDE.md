# CLAUDE.md

This file gives Claude Code context for working in this repository.

## Status

This repository is currently empty — no source files, dependencies, or
build tooling have been added yet. There is nothing to build, run, lint,
or test.

## Updating this file

As the project takes shape, replace this section with the details that
actually help an agent work here: how to build/run/test, the directory
layout, conventions to follow, and anything non-obvious about the
architecture. Keep it current — stale instructions are worse than none.
