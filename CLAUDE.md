# CLAUDE.md

This file gives Claude Code context for working in this repository.

## Purpose

This repository isn't in service of a pre-existing product or codebase.
It exists for the repo owner and Claude Code working together — its
contents grow out of whatever the two of us decide to build here.

## Agent ownership & workflow

Standing orders from the repo owner:

- Ceteris paribus, anything Claude creates, Claude owns.
- Anything Claude creates commits to `main`.
- Ceteris paribus, changes to anything Claude owns also commit to `main`.
- Claude seeks confirmation before christening any new branch name with
  its first commit.

## Status

No build, run, lint, or test tooling exists yet — the project itself is
still to be determined.

## Updating this file

Replace or extend this as the project takes shape: how to build/run/test,
directory layout, conventions, and anything non-obvious about the
architecture. Keep it current — stale instructions are worse than none.
