# Conformance pass — 25 Sep 2026, 21:51:57 BST

First compliance review of this repository, by session `5m0rwj`, against
`CLAUDE.md` and `READTHIS.md` as they stood at `441363e`.

## Scope and limits

This review is forensic. It inspects committed artifacts — files, paths,
commit messages, trailers, refs — and nothing else. It cannot observe a
sessionthread in flight, and says nothing about conduct between commits.

## Findings

**1. Attribution discriminator — PASS.** All four Claude-authored commits
carry a `Claude-Session:` trailer; all eighteen owner-authored commits
carry none. The distinction between "the owner did this" and "a thread
did this" is therefore recoverable from history alone, with no
ambiguity anywhere in the log.

**2. Rule files mutually consistent — PASS.** `README.md` defers to
`READTHIS.md`; `READTHIS.md` defers to `CLAUDE.md` for binding
instruction; `CLAUDE.md` declares the precedence order. Every
cross-reference resolves to a file that exists. No rule is stated
normatively in more than one place.

**3. Session-ID derivation was undocumented — FIXED at `441363e`.**
`CLAUDE.md` instructed threads to find the file addressed to them
without saying how to determine the ID identifying it. A dormant thread
could not have self-identified. Now stated: the last six characters of
the session name.

**4. Stray branch — OPEN, BLOCKED.** `claude/code-review-5m0rwj` remains
on origin at `05ca6dc`: an orphan root commit sharing no history with
`main`, holding a superseded draft of `CLAUDE.md`. It contradicts the
standing order that work goes to `main`, and a thread finding it might
reasonably take it for live. Deletion was authorised by the repo owner
and attempted four times; every attempt failed with
`send-pack: unexpected disconnect while reading sideband packet`, and
proxy diagnostics were unavailable to this session. Handed back to the
owner.

**5. `BSTworms/` convention was undocumented — FIXED this commit.**
Written up in `READTHIS.md`. This file is the first artifact filed under
it, and serves as a worked example.

## Note on this file

Filed under the write-once convention it documents. It is a snapshot of
one moment and will not be revised; a later pass gets a later file.
